Open the
[`memfault_platform_port.c`](https://github.com/memfault/memfault-firmware-sdk/blob/master/ports/templates/memfault_platform_port.c#L1)
copied into your `third_party/memfault` folder and fill out the stub
implementations accordingly.

#### Initialize Memfault Subsystem On Bootup

From your main routine, add a call to `memfault_platform_boot()` prior to the
startup of an RTOS or baremetal while loop.

```c
#include "memfault/components.h"

// ...

int main(void) {
  // ...
  memfault_platform_boot();
  // ...
}
```
