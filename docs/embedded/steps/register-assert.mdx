Memfault can automatically capture crash information on asserts by calling `MEMFAULT_ASSERT()` from your
platforms assert handler:

```c
#include "memfault/panics/assert.h"

#define YOUR_PLATFORM_ASSERT(x) MEMFAULT_ASSERT(x)
```
